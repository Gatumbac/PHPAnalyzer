# Web App (Scaffold)

This workspace is reserved for the React + Vite + TypeScript frontend.

Current status:
- Workspace initialized for pnpm monorepo usage.
- UI implementation is scheduled after analyzer extraction and API adapter work.
